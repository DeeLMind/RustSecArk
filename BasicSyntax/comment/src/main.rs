// ------------------ 普通注释 ------------------

// 单行注释：两个斜杠，常见
// 这是单行注释

/* 块注释：可以多行
   Rust 允许块注释嵌套
   /* 内部再嵌套一层 */ 
*/

// ------------------ 文档注释 ------------------

// 文档注释（行）：三个斜杠，用于给 rustdoc 提取
/// 这是函数的文档注释
/// 可以写 Markdown 格式
fn example() {}

/// # Example
/// ```
/// let x = 1 + 1;
/// assert_eq!(x, 2);
/// ```
fn with_code() {}

/*! 文档注释（块）：用于注释整个模块/包
   也支持 Markdown
*/

// ------------------ 属性内嵌文档 ------------------

// 用于模块、crate 级别文档
#![doc = "这是 crate 级别文档注释"]

// 用于函数、模块、结构体等
#[doc = "这是属性形式的文档注释"]
fn another_example() {}

// ------------------ 特殊注释 ------------------

// 在 unsafe 代码中常见
// SAFETY: 解释为什么这样用是安全的
unsafe fn dangerous() {}

// TODO: 任务标记
// FIXME: Bug 标记
// NOTE: 说明性注释

fn main() {
    println!("Hello, world!");
}
